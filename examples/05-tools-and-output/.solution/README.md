# Solution snapshot

Nothing to solve here. This example starts working and stays working, the same
way 02 does. The demo is Claude reaching for the tool, not a repair.

`tools/board.mjs` ships complete. `npm run reset -- 05` restores it along with
`data/board.json` if a run edits either one.

Do not edit anything here during a demo.
