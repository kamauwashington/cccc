---
description: Ask the third question of this example, about how busy the board is
allowed-tools: Bash(node tools/board.mjs:*), Bash(node tools/people.mjs:*), Bash(node tools/activity.mjs:*)
---

No reset. This runs in the same session as `/start`.

Open your reply with one line, before anything else:

```
You asked: Has the board cooled off, or is it still busy?
```

Then a blank line. Then answer that request by running the tool that matches
it, and follow the workspace rules.
