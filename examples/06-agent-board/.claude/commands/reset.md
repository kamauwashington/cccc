---
description: Restore this workspace from its .pristine snapshot
allowed-tools: Bash(npm run reset:*)
---

!`npm run reset`

The workspace is back to its starting state. Run `/clear` next so the
conversation forgets the previous attempt.
