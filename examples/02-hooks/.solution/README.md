# Solved state for 02-hooks

The `src/` tree after the demo runs cleanly:

- `src/schema.ts` has the `status` column.
- `src/generated/db-types.ts` was regenerated by `npm run codegen`, so
  `MessageColumn` and `MessageRow` both carry `status`.
- `src/handlers.ts` handles `'status'` in the `renderCell` switch, so the
  `assertNever` call compiles again.

Copy it over the live files when a live run stalls:

```
npm run solution -- 02
```

Then keep talking. `npm run reset -- 02` puts the starting state back.
