Goal: refactor the reporting module so every report shares one query layer.

Constraints:
- Rule 1: keep the exported helper in src/reports/helper-1.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 2: keep the exported helper in src/reports/helper-2.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 3: keep the exported helper in src/reports/helper-3.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 4: keep the exported helper in src/reports/helper-4.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 5: keep the exported helper in src/reports/helper-5.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 6: keep the exported helper in src/reports/helper-6.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 7: keep the exported helper in src/reports/helper-7.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 8: keep the exported helper in src/reports/helper-8.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 9: keep the exported helper in src/reports/helper-9.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 10: keep the exported helper in src/reports/helper-10.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 11: keep the exported helper in src/reports/helper-11.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 12: keep the exported helper in src/reports/helper-12.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 13: keep the exported helper in src/reports/helper-13.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 14: keep the exported helper in src/reports/helper-14.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 15: keep the exported helper in src/reports/helper-15.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 16: keep the exported helper in src/reports/helper-16.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 17: keep the exported helper in src/reports/helper-17.ts exactly where it is today, and do not rename any of the arguments that it takes.
- Rule 18: keep the exported helper in src/reports/helper-18.ts exactly where it is today, and do not rename any of the arguments that it takes.

Output format: one diff per file, in the order the files are listed above.

Success criteria: `npm test` passes and the report suite runs in under 30 seconds.

What to avoid: do not add a new dependency, and do not change the database schema.
