---
description: Rewrite text so it passes the repository writing rules
argument-hint: [the text, or a file path]
allowed-tools: Skill, Read, Write, Bash(npx tsx src/cli.ts:*)
---

Use the `concise` skill on the text below. Follow the skill exactly, including
the checker run at the end.

$ARGUMENTS
