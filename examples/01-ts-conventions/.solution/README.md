# Solution snapshot

The finished `src/` for 01-ts-conventions. Three TypeScript enums converted to
the const object plus union pattern from the `ts-conventions` skill.

Use it as the live demo fallback:

```
npm run solution -- 01
```

That copies `src/` over the live files. `npm run reset -- 01` puts the enum
based starting state back.

Do not edit anything here during a demo.
